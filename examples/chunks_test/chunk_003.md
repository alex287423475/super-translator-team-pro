场景：代码块中的内容。

原文：
Calculate the sum
def add(a, b):
    return a + b

错误译文：
计算总和
def 加(a, b):
    return a + b

正确译文：
Calculate the sum
def add(a, b):
    return a + b

解析：
绝对保护：代码块内的任何内容（包括注释 #、函数名 add、变量名 a、b）都严禁修改。这是不可逾越的红线。